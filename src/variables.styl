/**
 * Copyright 2015 Google Inc. All Rights Reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/*------------------------------------*\
    $CONTENTS
\*------------------------------------*/
/**
 * STYLE GUIDE VARIABLES------------------Declarations of Sass variables
 * -----Typography
 * -----Colors
 * -----Textfield
 * -----Switch
 * -----Spinner
 * -----Radio
 * -----Menu
 * -----List
 * -----Layout
 * -----Icon toggles
 * -----Footer
 * -----Column
 * -----Checkbox
 * -----Card
 * -----Button
 * -----Animation
 * -----Progress
 * -----Badge
 * -----Shadows
 * -----Grid
 * -----Data table
 *
 * Even though all variables have the `!default` directive, most of them
 * should not be changed as they are dependent one another. This can cause
 * visual distortions (like alignment issues) that are hard to track down
 * and fix.
 */


/* ==========  TYPOGRAPHY  ========== */

/* We're splitting fonts into "preferred" and "performance" in order to optimize
   page loading. For important text, such as the body, we want it to load
   immediately and not wait for the web font load, whereas for other sections,
   such as headers and titles, we're OK with things taking a bit longer to load.
   We do have some optional classes and parameters in the mixins, in case you
   definitely want to make sure you're using the preferred font and don't mind
   the performance hit.
   We should be able to improve on this once CSS Font Loading L3 becomes more
   widely available.
*/
$preferred_font ?= 'Roboto', 'Helvetica', 'Arial', sans-serif;
$performance_font ?= 'Helvetica', 'Arial', sans-serif;

/* ==========  COLORS  ========== */

/**
*
* Material design color palettes.
* @see http://www.google.com/design/spec/style/color.html
*
**/

@import "color-definitions";
@import "functions";

/* ==========  Color & Themes  ========== */

// Define whether individual color pallet items should have classes created.
// Setting this to true will remove individual color classes for each color in the pallets.
// To improve overall performance (assuming they aren't used) by:
// * Saving server bandwidth sending the extra classes
// * Save client computation against the classes
// it is RECOMMENDED you set this to true.
$trim-color-classes ?= false;

// Use color primarily for emphasis. Choose colors that fit with
// your brand and provide good contrast between visual components.
$color-primary ?= $palette-indigo-500;
$color-primary-dark ?= $palette-indigo-700;
$color-accent ?= $palette-pink-A200;

// Our primary is dark, so use $color-dark-contrast for overlaid text.
$color-primary-contrast ?= $color-dark-contrast;
// Our accent is dark, so use $color-dark-contrast for overlaid text.
$color-accent-contrast ?= $color-dark-contrast;

// Replace all colors with placeholders if we're generating a template.
if $styleguide-generate-template == true {
  $color-primary: '$color-primary';
  $color-primary-dark: '$color-primary-dark';
  $color-accent: '$color-accent';
  $color-primary-contrast: '$color-primary-contrast';
  $color-accent-contrast: '$color-accent-contrast';
}

/* ==========  Typography  ========== */

// We use the following default color styles: text-color-primary and
// text-color-secondary. For light themes, use text-color-primary-inverse
// and text-color-secondary-inverse.

$text-color-primary ?= convert("rgba(" + $color-black + ", 0.87)");
$text-link-color ?= convert("rgb(" + $color-accent + ")");

// Define whether to target elements directly for typographic enhancements.
// Turning this off means you need to use mdl-* classes more often.
// Other components may also fail to adhere to MD without these rules.
// It is strongly recommended you leave this as true.

$target-elements-directly ?= true;

/* ==========  Components  ========== */

/* ==========  Standard Buttons  ========== */

// Default button colors.
$button-primary-color ?= convert("rgba(" + $palette-grey-500 + ", 0.20)");
$button-secondary-color ?= convert("rgb(" + $color-black + ")");
$button-hover-color ?= $button-primary-color;
$button-active-color ?= convert("rgba(" + $palette-grey-500 + ", 0.40)");
$button-focus-color ?= convert("rgba(" + $color-black + ", 0.12)");

// Colored button colors.
$button-primary-color-alt ?= convert("rgb(" + $color-primary + ")");
$button-secondary-color-alt ?= convert("rgb(" + $color-primary-contrast + ")");
$button-hover-color-alt ?= convert("rgb(" + $color-primary + ")");
$button-active-color-alt ?= convert("rgb(" + $color-primary + ")");
$button-focus-color-alt ?= $button-focus-color;

// Ripple color for colored raised buttons.
$button-ripple-color-alt ?= convert("rgb(" + $color-primary-contrast + ")");

// Disabled button colors.
$button-primary-color-disabled ?= convert("rgba(" + $color-black + ", 0.12)");
$button-secondary-color-disabled ?= convert("rgba(" + $color-black + ", 0.26)");

// FAB colors and sizes.
$button-fab-color-alt ?= convert("rgb(" + $color-accent + ")");
$button-fab-hover-color-alt ?= convert("rgb(" + $color-accent + ")");
$button-fab-active-color-alt ?= convert("rgb(" + $color-accent + ")");
$button-fab-text-color-alt ?= convert("rgb(" + $color-accent-contrast + ")");
$button-fab-ripple-color-alt ?= convert("rgb(" + $color-accent-contrast + ")");

// Icon button colors and sizes.
$button-icon-color ?= convert("rgb(" + $palette-grey-700 + ")");
$button-icon-focus-color ?= $button-focus-color;

/* ==========  Icon Toggles  ========== */

$icon-toggle-color ?= convert("rgb(" + $palette-grey-700 + ")");
$icon-toggle-focus-color ?= $button-focus-color;
$icon-toggle-checked-color ?= convert("rgb(" + $color-primary + ")");
$icon-toggle-checked-focus-color ?= convert("rgba(" + $color-primary + ", 0.26)");
$icon-toggle-disabled-color ?= convert("rgba(" + $color-black + ", 0.26)");

/* ==========  Radio Buttons  ========== */

$radio-color ?= convert("rgb(" + $color-primary + ")");
$radio-off-color ?= convert("rgba(" + $color-black + ", 0.54)");
$radio-disabled-color ?= convert("rgba(" + $color-black + ", 0.26)");

/* ==========  Ripple effect  ========== */

$ripple-bg-color ?= convert("rgb(" + $color-light-contrast + ")");

/* ==========  Layout  ========== */

$layout-nav-color ?= convert("rgb(" + $palette-grey-300 + ")");

// Drawer
$layout-drawer-bg-color ?= convert("rgb(" + $palette-grey-50 + ")");
$layout-drawer-border-color ?= convert("rgb(" + $palette-grey-300 + ")");
$layout-text-color ?= convert("rgb(" + $palette-grey-800 + ")");
$layout-drawer-navigation-color ?= #757575;
$layout-drawer-navigation-link-active-background ?= convert("rgb(" + $color-light-contrast + ")");
$layout-drawer-navigation-link-active-color ?= convert("rgb(" + $color-primary + ")");

// Header
$layout-header-bg-color ?= convert("rgb(" + $color-primary + ")");
$layout-header-text-color ?= convert("rgb(" + $color-primary-contrast + ")");
$layout-header-nav-hover-color ?= convert("rgba(" + $palette-grey-700 + ", 0.6)");
$layout-header-tab-text-color ?= convert("rgba(" + $color-primary-contrast + ", 0.6)");

// Tabs
$layout-header-tab-highlight ?= convert("rgb(" + $color-accent + ")");

/* ==========  Content Tabs  ========== */

$tab-highlight-color ?= convert("rgb(" + $color-primary + ")");
$tab-text-color ?= convert("rgba(" + $color-black + ", 0.54)");
$tab-active-text-color ?= convert("rgba(" + $color-black + ", 0.87)");
$tab-border-color ?= convert("rgb(" + $palette-grey-300 + ")");

/* ==========  Checkboxes  ========== */

$checkbox-color ?= convert("rgb(" + $color-primary + ")");
$checkbox-off-color ?= convert("rgba(" + $color-black + ", 0.54)");
$checkbox-disabled-color ?= convert("rgba(" + $color-black + ", 0.26)");
$checkbox-focus-color ?= convert("rgba(" + $color-primary + ", 0.26)");

/* ==========  Switches  ========== */

$switch-color ?= convert("rgb(" + $color-primary + ")");
$switch-faded-color ?= convert("rgba(" + $color-primary + ", 0.26)");
$switch-thumb-color ?= $switch-color;
$switch-track-color ?= convert("rgba(" + $color-primary + ", 0.5)");

$switch-off-thumb-color ?= convert("rgb(" + $palette-grey-50 + ")");
$switch-off-track-color ?= convert("rgba(" + $color-black + ", 0.26)");
$switch-disabled-thumb-color ?= convert("rgb(" + $palette-grey-400 + ")");
$switch-disabled-track-color ?= convert("rgba(" + $color-black + ", 0.12)");

/* ==========  Spinner  ========== */

$spinner-color-1 ?= convert("rgb(" + $palette-blue-400 + ")");
$spinner-color-2 ?= convert("rgb(" + $palette-red-500 + ")");
$spinner-color-3 ?= convert("rgb(" + $palette-yellow-600 + ")");
$spinner-color-4 ?= convert("rgb(" + $palette-green-500 + ")");

$spinner-single-color ?= convert("rgb(" + $color-primary + ")");

/* ==========  Text fields  ========== */

$input-text-background-color ?= transparent;
$input-text-label-color ?= convert("rgba(" + $color-black + ", 0.26)");
$input-text-bottom-border-color ?= convert("rgba(" + $color-black + ", 0.12)");
$input-text-highlight-color ?= convert("rgb(" + $color-primary + ")");
$input-text-disabled-color ?= $input-text-bottom-border-color;
$input-text-disabled-text-color ?= $input-text-label-color;
$input-text-error-color ?= unquote("rgb(222, 50, 38)");

/* ==========  Card  ========== */

$card-background-color ?= convert("rgb(" + $color-white + ")");
$card-text-color ?= convert("rgb(" + $color-black + ")");
$card-image-placeholder-color ?= convert("rgb(" + $color-accent + ")");
$card-supporting-text-text-color ?= convert("rgba(" + $color-black + ", 0.54)");
$card-border-color ?= rgba(0,0,0,0.1);

/* ==========  Sliders ========== */

$range-bg-color ?= convert("rgba(" + $color-black + ", 0.26)");
$range-color ?= convert("rgb(" + $color-primary + ")");
$range-faded-color ?= convert("rgba(" + $color-primary + ", 0.26)");
$range-bg-focus-color ?= convert("rgba(" + $color-black + ", 0.12)");

/* ========== Progress ========== */
$progress-main-color ?= convert("rgb(" + $color-primary + ")");
$progress-secondary-color ?= convert("rgba(" + $color-primary-contrast + ", 0.7)");
$progress-fallback-buffer-color ?= convert("rgba(" + $color-primary-contrast + ", 0.9)");

/* ==========  List ========== */

$list-container-item-border-color ?= convert("rgb(" + $palette-grey-300 + ")");
$list-container-view-background ?= convert("rgb(" + $color-white + ")");

/* ==========  Item ========== */

// Default Item Colors
$default-item-text-color ?= convert("rgba(" + $color-black + ", 0.87)");
$default-item-outline-color ?= convert("rgb(" + $palette-grey-400 + ")");
$default-item-hover-bg-color ?= convert("rgb(" + $palette-grey-200 + ")");
$default-item-focus-bg-color ?= convert("rgb(" + $palette-grey-200 + ")");
$default-item-active-bg-color ?= convert("rgb(" + $palette-grey-300 + ")");

// Disabled Button Colors
$disabled-item-text-color ?= convert("rgb(" + $palette-grey-400 + ")");

/* ==========  Dropdown menu ========== */

$default-dropdown-bg-color ?= convert("rgb(" + $color-white + ")");

/* ==========  Tooltips  ========== */

$tooltip-text-color ?= convert("rgb(" + $color-white + ")");
$tooltip-background-color ?= convert("rgba(" + $palette-grey-700 + ", 0.9)");

/* ==========  Footer  ========== */

$footer-bg-color ?= convert("rgb(" + $palette-grey-800 + ")");
$footer-color ?= convert("rgb(" + $palette-grey-500 + ")");
$footer-heading-color ?= convert("rgb(" + $palette-grey-300 + ")");
$footer-button-fill-color ?= $footer-color;
$footer-underline-color ?= $footer-color;


/* TEXTFIELD */

$input-text-font-size ?= 16px;
$input-text-width ?= 100%;
$input-text-padding ?= 4px;
$input-text-vertical-spacing ?= 20px;

$input-text-button-size ?= 32px;
$input-text-floating-label-fontsize ?= 12px;
$input-text-expandable-icon-top ?= 16px;


/* SWITCH */

$switch-label-font-size ?= 16px;
$switch-label-height ?= 24px;
$switch-track-height ?= 14px;
$switch-track-length ?= 36px;
$switch-thumb-size ?= 20px;
$switch-track-top ?= ($switch-label-height - $switch-track-height) / 2;
$switch-thumb-top ?= ($switch-label-height - $switch-thumb-size) / 2;
$switch-ripple-size ?= $switch-label-height * 2;
$switch-helper-size ?= 8px;

/* SPINNER */

$spinner-size ?= 28px;
$spinner-stroke-width ?= 3px;

// Amount of circle the arc takes up.
$spinner-arc-size ?= 270deg;
// Time it takes to expand and contract arc.
$spinner-arc-time ?= 1333ms;
// How much the start location of the arc should rotate each time.
$spinner-arc-start-rot ?= 216deg;

$spinner-duration ?= unit(360 * $spinner-arc-time / ( strip-units($spinner-arc-start-rot + (360deg - $spinner-arc-size))), ms);


/* RADIO */

$radio-label-font-size ?= 16px;
$radio-label-height ?= 24px;
$radio-button-size ?= 16px;
$radio-inner-margin ?= $radio-button-size / 4;
$radio-padding ?= 8px;
$radio-top-offset ?= ($radio-label-height - $radio-button-size) / 2;
$radio-ripple-size ?= 42px;


/* MENU */

$menu-expand-duration ?= 0.3s;
$menu-fade-duration ?= 0.2s;

/* LIST */

$list-container-height ?= 500px;
$list-container-width ?= 400px;
$list-container-item-height ?= 70px;

/* LAYOUT */

$layout-nav-link-font-size ?= 13px;

$layout-drawer-narrow ?= 240px;
$layout-drawer-wide ?= 456px;
$layout-drawer-width ?= $layout-drawer-narrow;

$layout-header-icon-size ?= 32px;
$layout-screen-size-threshold ?= 1024px;
$layout-header-icon-margin ?= 24px;
$layout-drawer-button-mobile-size ?= 32px;
$layout-drawer-button-desktop-size ?= 48px;

$layout-header-mobile-row-height ?= 56px;
$layout-mobile-header-height ?= $layout-header-mobile-row-height;
$layout-header-desktop-row-height ?= 64px;
$layout-desktop-header-height ?= $layout-header-desktop-row-height;

$layout-header-desktop-baseline ?= 80px;
$layout-header-mobile-baseline ?= 72px;
$layout-header-mobile-indent ?= 16px;
$layout-header-desktop-indent ?= 40px;

$layout-tab-font-size ?= 14px;
$layout-tab-bar-height ?= 48px;
$layout-tab-mobile-padding ?= 12px;
$layout-tab-desktop-padding ?= 24px;
$layout-tab-highlight-thickness ?= 2px;


/* ICON TOGGLE */

$icon-toggle-size ?= 32px;
$icon-toggle-font-size ?= 24px;
$icon-toggle-ripple-size ?= 36px;

/* FOOTER */

/*mega-footer*/
$footer-min-padding ?= 16px;
$footer-padding-sides ?= 40px;
$footer-heading-font-size ?= 14px;
$footer-heading-line-height ?= (1.7 * $footer-heading-font-size);
$footer-btn-size ?= 36px ;

/*mini-footer*/
$padding ?= 16px;
$footer-heading-font-size ?= 24px;
$footer-heading-line-height ?= (1.5 * $footer-heading-font-size);
$footer-btn-size ?= 36px;

/* CHECKBOX */

$checkbox-label-font-size ?= 16px;
$checkbox-label-height ?= 24px;
$checkbox-button-size ?= 16px;
$checkbox-inner-margin ?= 2px;
$checkbox-padding ?= 8px;
$checkbox-top-offset ?= ($checkbox-label-height - $checkbox-button-size - $checkbox-inner-margin) / 2;
$checkbox-ripple-size ?= $checkbox-label-height * 1.5;

/* CARD */

/* Card dimensions */
$card-width ?= 330px;
$card-height ?= 200px;
$card-font-size ?= 16px;
$card-title-font-size ?= 24px;
$card-subtitle-font-size ?= 14px;
$card-horizontal-padding ?= 16px;
$card-vertical-padding ?= 16px;

$card-title-perspective-origin-x ?= 165px;
$card-title-perspective-origin-y ?= 56px;

$card-title-transform-origin-x ?= 165px;
$card-title-transform-origin-y ?= 56px;

$card-title-text-transform-origin-x ?= 149px;
$card-title-text-transform-origin-y ?= 48px;

$card-supporting-text-font-size ?= 13px;
$card-supporting-text-line-height ?= 18px;

$card-actions-font-size ?= 16px;

$card-title-text-font-weight ?= 300;
$card-z-index ?= 1;

/* Cover image */
$card-cover-image-height ?= 186px;
$card-background-image-url ?= '';


/* BUTTON */
/**
 *
 * Dimensions
 *
 */
$button-min-width ?= 64px;
$button-height ?= 36px;
$button-padding ?= 8px;
$button-margin ?= 4px;
$button-border-radius ?= 2px;

$button-fab-size ?= 56px;
$button-fab-size-mini ?= 40px;
$button-fab-font-size ?= 24px;

$button-icon-size ?= 32px;
$button-icon-size-mini ?= 24px;


/* ANIMATION */
$animation-curve-fast-out-slow-in ?= cubic-bezier(0.4, 0, 0.2, 1);
$animation-curve-linear-out-slow-in ?= cubic-bezier(0, 0, 0.2, 1);
$animation-curve-fast-out-linear-in ?= cubic-bezier(0.4, 0, 1, 1);

$animation-curve-default ?= $animation-curve-fast-out-slow-in;


/* PROGRESS */
$bar-height ?= 4px;

/* BADGE */
$badge-font-size ?= 12px;
$badge-color ?= convert("rgb(" + $color-white + ")");
$badge-color-inverse ?= convert("rgb(" + $color-accent + ")");
$badge-background ?= convert("rgb(" + $color-accent + ")");
$badge-background-inverse ?= convert("rgba(" + $color-white + ",0.2)");
$badge-size ?= 22px;
$badge-padding ?= 2px;

/* SHADOWS */

$shadow-key-umbra-opacity ?= 0.2;
$shadow-key-penumbra-opacity ?= 0.14;
$shadow-ambient-shadow-opacity ?= 0.12;

/* GRID */

$grid-desktop-columns ?= 12;
$grid-desktop-gutter ?= 16px;
$grid-desktop-margin ?= 16px;

$grid-desktop-breakpoint ?= 840px;

$grid-tablet-columns ?= 8;
$grid-tablet-gutter ?= $grid-desktop-gutter;
$grid-tablet-margin ?= $grid-desktop-margin;

$grid-tablet-breakpoint ?= 480px;

$grid-phone-columns ?= 4;
$grid-phone-gutter ?= $grid-desktop-gutter;
$grid-phone-margin ?= $grid-desktop-margin;

$grid-cell-default-columns ?= $grid-phone-columns;
$grid-max-columns ?= $grid-desktop-columns;

/* DATA TABLE */

$data-table-font-size ?= 13px;
$data-table-header-font-size ?= 12px;

$data-table-header-color ?= rgba(#000, 0.54);
$data-table-divider-color ?= rgba(#000, 0.12);

$data-table-hover-color ?= #eeeeee;
$data-table-selection-color ?= #e0e0e0;

$data-table-dividers ?= 1px solid $data-table-divider-color;

$data-table-row-height ?= 48px;
$data-table-last-row-height ?= 56px;
$data-table-header-height ?= 56px;

$data-table-column-spacing ?= 36px;
$data-table-column-padding ?= $data-table-column-spacing / 2;

$data-table-card-header-height ?= 64px;
$data-table-card-title-top ?= 20px;
$data-table-card-padding ?= 24px;
$data-table-button-padding-right ?= 16px;
$data-table-cell-top ?= $data-table-card-padding / 2;

/* TOOLTIP */
$tooltip-font-size ?= 10px;
$tooltip-font-size-large ?= 14px;
